# JavaScript Modules & Patterns

ES modules: static import/export, tree-shakeable. CommonJS: require/module.exports, dynamic loading. Module patterns: IIFE for scope isolation, Revealing Module pattern (return only public API), Singleton via module caching (same instance on multiple require). Dynamic import() returns Promise -- code splitting. Named vs default exports: named preferred for tools/libraries, default for single-export modules.
