# hashlib-module

hashlib: 1) hashlib.sha256(data).hexdigest(). 2) hashlib.md5. 3) hashlib.pbkdf2_hmac for password hashing. 4) hashlib.blake2b. 5) Streaming: h.update(chunk). 6) Comparison: hmac.compare_digest.
