# js-modules

ES Modules: 1) export const x = 5. 2) export default function. 3) import { x } from './mod'. 4) import * as mod from './mod'. 5) Dynamic import() for lazy. 6) Re-export: export { x } from './mod'. Tree shaking works with named exports.
