# js-prototypes

JS Prototypes: every object has __proto__. Prototype chain: obj.method() walks up chain. class syntax is syntactic sugar over prototypes. Object.create(proto) for manual chain. hasOwnProperty check. Modern: use classes, avoid manual prototype manipulation.
