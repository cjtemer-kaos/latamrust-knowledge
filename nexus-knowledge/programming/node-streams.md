# node-streams

Node streams: 1) Readable, Writable, Duplex, Transform. 2) pipe() for chaining. 3) backpressure handling. 4) fs.createReadStream. 5) crypto.createHash. 6) zlib.createGzip. 7) pipeline() for error handling.
