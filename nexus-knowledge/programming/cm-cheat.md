# cm-cheat

CM: with open('f') as fh: data = fh.read(). with contextmanager: @contextmanager def cm(): setup(); yield; teardown(). with suppress(Exception): risky_code(). with redirect_stdout(io.StringIO()): print().
