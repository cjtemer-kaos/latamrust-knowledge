# algorithm-time-complexity

Big O notation: 1) O(1) constant. 2) O(log n) logarithmic (binary search). 3) O(n) linear. 4) O(n log n) linearithmic (merge sort). 5) O(n²) quadratic (bubble sort). 6) O(2ⁿ) exponential (recursion). 7) O(n!) factorial (permutations). Space complexity matters too.
