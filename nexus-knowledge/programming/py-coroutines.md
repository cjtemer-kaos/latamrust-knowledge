# py-coroutines

Coroutines: async def crea coroutine. await pausa. asyncio.gather para paralelo. asyncio.wait_for timeout. asyncio.shield para no cancelar. asyncio.Semaphore para limitar concurrencia.
