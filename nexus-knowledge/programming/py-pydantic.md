# py-pydantic

Pydantic: validación de datos con types. class User(BaseModel): name: str; age: int; email: str. Valida al instanciar. Convierte tipos automáticamente. Nested models. Custom validators con @validator. Essential para APIs REST.
