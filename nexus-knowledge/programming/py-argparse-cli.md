# py-argparse-cli

CLI: import argparse; parser = argparse.ArgumentParser(); parser.add_argument('name'); parser.add_argument('--count', type=int, default=1); args = parser.parse_args(); for _ in range(args.count): print(f'Hello {args.name}!').
