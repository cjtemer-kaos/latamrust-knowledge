# py-abc

Abstract Base Classes: from abc import ABC, abstractmethod. class Shape(ABC): @abstractmethod def area(self): .... Impide instanciar clases incompletas. Útil para interfaces. Python no tiene interfaces nativas, ABC las simula.
