# subprocess-cheat

subprocess: import subprocess. subprocess.run(['ls', '-la'], capture_output=True, text=True). subprocess.check_output(['pwd']). subprocess.check_call(['echo', 'hello']). Popen for low-level.
