# py-walrus

Walrus operator (:=): x := expr evalúa y asigna. while chunk := file.read(8192): process(chunk). if (n := len(data)) > 1000: print(f'Large: {n}'). Clean up repetitive assignments. Python 3.8+.
