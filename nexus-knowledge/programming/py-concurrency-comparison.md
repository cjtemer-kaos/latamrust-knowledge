# py-concurrency-comparison

Python concurrency: 1) threading: I/O-bound, GIL limits. 2) multiprocessing: CPU-bound. 3) asyncio: I/O, single-threaded. 4) concurrent.futures: ThreadPoolExecutor/ProcessPoolExecutor. 5) GIL: one thread executes Python bytecode at a time.
