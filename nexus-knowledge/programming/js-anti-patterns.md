# js-anti-patterns

JS anti-patterns: 1) var usage. 2) == instead of ===. 3) for...in on arrays. 4) Callback hell (use async/await). 5) Global variables. 6) Memory leaks (detached DOM). 7) Synchronous XHR.
