# py-debugging

Python debugging: 1) pdb/pdb++. 2) breakpoint(). 3) IDE debugger. 4) logging for tracing. 5) print debugging (last resort). 6) remote-pdb. 7) py-spy sampling. 8) faulthandler for segfaults.
