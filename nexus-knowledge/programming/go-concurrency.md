# go-concurrency

Go concurrency: 1) goroutines: go func(). 2) channels: make(chan int). 3) select for multiplexing. 4) sync.WaitGroup. 5) sync.Mutex. 6) context for cancellation. 7) errgroup for error handling.
