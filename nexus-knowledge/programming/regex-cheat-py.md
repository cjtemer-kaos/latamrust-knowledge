# regex-cheat-py

re: re.search(pattern, string) find first. re.findall(pattern, string) all matches. re.sub(pattern, replacement, string) replace. re.split(pattern, string) split. re.compile(pattern) reusable. (?P<name>group) named group.
