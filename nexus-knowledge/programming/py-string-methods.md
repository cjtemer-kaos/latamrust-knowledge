# py-string-methods

String methods: 1) split, join. 2) strip, lstrip, rstrip. 3) upper, lower, title. 4) find, index, count. 5) replace. 6) startswith, endswith. 7) format, f-strings. 8) encode, decode.
