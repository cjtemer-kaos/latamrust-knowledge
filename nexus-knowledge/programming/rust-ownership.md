# rust-ownership

Rust ownership: 1) One owner per value. 2) Move semantics. 3) Borrowing: &T (shared) &mut T (exclusive). 4) Lifetimes: 'a ensures references valid. 5) No data races at compile time. 6) Drop for cleanup.
