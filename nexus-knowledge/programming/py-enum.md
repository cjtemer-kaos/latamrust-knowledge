# py-enum

Python enums: from enum import Enum, auto. class Color(Enum): RED = auto(); GREEN = auto(). Comparison: Color.RED == Color.RED. Iteration: for c in Color. Useful for constants, state machines, type-safe options.
