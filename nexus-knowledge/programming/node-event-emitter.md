# node-event-emitter

EventEmitter: const EventEmitter = require('events'); const emitter = new EventEmitter(). emitter.on('event', handler). emitter.emit('event', data). emitter.once(). emitter.removeListener(). Error event handling.
