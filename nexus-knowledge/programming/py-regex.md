# py-regex

Python re module: 1) re.compile for reuse. 2) Named groups: (?P<name>pattern). 3) Lookahead: (?=pattern). 4) Lookbehind: (?<=pattern). 5) re.sub for replace. 6) re.split. 7) re.findall. 8) re.DOTALL for multiline. 9) Non-greedy: *? +?.
