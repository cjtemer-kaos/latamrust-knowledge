# py-logging-setup

Logging: import logging; logging.basicConfig(level=logging.INFO, format='%(asctime)s %(levelname)s %(message)s'); logger = logging.getLogger(__name__); logger.info('Starting'); try: ... except Exception: logger.exception('Error').
