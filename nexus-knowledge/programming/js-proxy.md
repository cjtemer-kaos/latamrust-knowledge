# js-proxy

JS Proxy: new Proxy(target, handler). handler traps: get, set, has, deleteProperty, apply, construct. Use cases: validation, logging, reactive data, default values, negative array indexing. Vue 3 reactivity uses Proxies.
