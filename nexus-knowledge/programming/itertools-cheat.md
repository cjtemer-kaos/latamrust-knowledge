# itertools-cheat

itertools: chain(a, b) flatten. product(a, b) cartesian. combinations(seq, 2) pairs. permutations(seq, 2) ordered pairs. islice(iter, 5) first 5. groupby(sorted) consecutive groups. cycle(iter) infinite repeat.
