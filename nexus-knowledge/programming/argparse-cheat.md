# argparse-cheat

argparse: parser = argparse.ArgumentParser(). parser.add_argument('name'). parser.add_argument('--flag', action='store_true'). parser.add_argument('-n', '--number', type=int). args = parser.parse_args().
