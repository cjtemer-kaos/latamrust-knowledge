# Compiler Design — LLVM IR, SSA, Optimization Passes y MLIR

# Diseno de compiladores modernos: el pipeline clasico es Source Code -> Lexer/Parser -> AST -> Interm

Diseno de compiladores modernos: el pipeline clasico es Source Code -> Lexer/Parser -> AST -> Intermediate Representation (IR) -> Optimized IR -> Machine Code (Assembly). LLVM (Low-Level Virtual Machine, Chris Lattner 2003) es la infraestructura de compilacion modular mas importante. LLVM IR: repres

## Temas


## Codigo

