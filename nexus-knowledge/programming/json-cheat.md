# json-cheat

json: import json. json.dumps(obj) object→string. json.loads(str) string→object. json.dump(obj, file) object→file. json.load(file) file→object. json.dumps(obj, indent=2) pretty. json.dumps(obj, ensure_ascii=False) unicode.
