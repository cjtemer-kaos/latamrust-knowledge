# error-cheat

Errors: try: code. except ValueError as e: handle(e). except (TypeError, KeyError): handle_multiple(). else: no_error_code. finally: cleanup_code. raise ValueError('msg'). raise from e.
