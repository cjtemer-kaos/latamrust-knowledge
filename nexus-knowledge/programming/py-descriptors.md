# py-descriptors

Python descriptors: __get__, __set__, __delete__. Implementan comportamiento de atributos. @property es un descriptor. Validadores: class Validator: def __set_name__(self, owner, name). Clases ORM como SQLAlchemy usan descriptors masivamente.
