# py-pathlib

pathlib: from pathlib import Path. p = Path('.'). p / 'sub' / 'file.txt'. p.exists(), p.read_text(), p.write_text(). p.glob('*.py'). p.resolve(). p.stat().st_size. Modern replacement for os.path. Cross-platform.
