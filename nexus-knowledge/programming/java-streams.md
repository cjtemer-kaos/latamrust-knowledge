# java-streams

Java Streams: 1) stream().filter().map().collect(). 2) forEach. 3) reduce. 4) flatMap. 5) sorted. 6) distinct. 7) peek. 8) Optional for null safety. 9) Collectors: groupingBy, toMap. 10) Parallel streams.
