# GraphQL — Apollo Federation, Subscriptions, Schema Design, vs REST

# GraphQL: lenguaje de consulta para APIs creado por Facebook (2015). Unico endpoint, cliente especifi

GraphQL: lenguaje de consulta para APIs creado por Facebook (2015). Unico endpoint, cliente especifica estructura de datos exacta, elimina over-fetching y under-fetching. Componentes: Schema Definition Language (SDL) con tipos escalares (String, Int, Float, Boolean, ID) y tipos objeto, Query (lectur

## Temas


## Codigo

