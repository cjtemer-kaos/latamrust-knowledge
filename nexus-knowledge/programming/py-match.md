# py-match

Match-case (Python 3.10+): match command: case 'quit': exit() case ['load', file]: load(file) case _: default(). Structural pattern matching. Guards: case x if x > 0. Replaces many if/elif chains.
