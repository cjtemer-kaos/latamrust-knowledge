# datetime-cheat

datetime: from datetime import datetime, timedelta, date. datetime.now(). datetime.now(timezone.utc). date.today(). datetime(2024, 1, 1). timedelta(days=7). dt.isoformat(). dt.strftime('%Y-%m-%d').
