# py-functools

functools: 1) lru_cache(maxsize=128): memoization. 2) partial: fix arguments. 3) reduce: fold iterable. 4) total_ordering: auto-comparison methods. 5) wraps: preserve function metadata in decorators. 6) cached_property: lazy compute once.
