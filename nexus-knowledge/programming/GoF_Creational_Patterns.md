# GoF Creational Patterns

Singleton: one instance, Python modules are naturally singletons. Factory Method: define interface for object creation, subclasses decide concrete class. Abstract Factory: factory of factories for related object families. Builder: separate construction from representation (Director+Builder). Prototype: clone via copy.deepcopy(). Python dataclass replace() is Prototype. Registry: name->class dict for dynamic dispatch (Django URL resolvers).
