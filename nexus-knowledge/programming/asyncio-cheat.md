# asyncio-cheat

asyncio: async def func(): await something(). await asyncio.gather(task1, task2). await asyncio.wait_for(coro, timeout). asyncio.create_task(coro). asyncio.Queue(). asyncio.Semaphore().
