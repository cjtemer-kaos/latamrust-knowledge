# supernexus-herramienta-guardar-conocimiento

COMO INYECTAR CONOCIMIENTO al cerebro compartido: from src.brain.cerebro import Cerebro; c=Cerebro(); await c.guardar_conocimiento(tema, contenido, fuente, utilidad). METODO ASYNC: debe usar await o asyncio.run(). DESDE HERMES MCP: brain_remember(topic, content) o add_observation(content, category, topic_key). CUANDO INYECTAR: cada vez que se descubre algo nuevo, se resuelve bug importante, se aprende leccion, o el usuario ensena algo.
