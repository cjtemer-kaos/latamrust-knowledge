# Direct: estudio_asistentes_ide_ia_completo

Category: Learned
Tags: direct-learned
Created: 2026-08-16T11:47:50.745178

---

ESTUDIO COMPLETO asistentes IDE/IA (2026-08-16, 4 fuentes): (1) deepseek-harness = AGENT HARNESS standalone (NO RL): event-sourced session log como fuente de verdad, capability seams (todo plugin sobre Cordis), tool pipeline con guards monotonicos+approval+fs-write-intent, sandbox multi-SO (bwrap/Landlock/Seatbelt/Windows ACL), subagent providers (spawn/fork/claude-code/codex/acp), workflow engine con scripts modelo-escritos (Ralph loop: status/summary/evidence/next-steps/blocker), compaction, hooks bridges compatibles con claude-code y codex. Requisitos: Node 22+pnpm+API key. (2) prime-agent: loop agnóstico de proveedor (AgentLoopConfig hooks), Continual Harness = harness_state.json {schema:1, entries:{prompt,memory,skill,subagent}, refinements} simetrico Python(harness.py stdlib)/TS(refinement.ts, escritura atomica temp+rename), /refine emite JSON edits, skills = SKILL.md+pyproject+src/__init__ instaladas editable en venv uv, subagentes = await rlm() solo admision (ActionStore lifecycle queued->running->completed, dos lanes steering/followUp), sessions JSONL append-only versionado, daemon socket local, autonomo con quality gates + git worktree snapshot. NO usar: daemon completo, DB acoplada, wrappers no nativos. (3) Hermes (mi sistema): AIAgent run_agent.py:412, conversation_loop.py:1422 con iteration_budget y validacion defensiva de tool calls (reparar nombres JSON, 3 reintentos), skills como DATOS (SKILL.md frontmatter), memoria MEMORY.md/USER.md con snapshot congelado para prompt-cache + estado vivo, subagentes = instancias AIAgent aisladas con flags (skip_memory/platform/depth), MCP = 1 Task asyncio por servidor + lock RPC + cooldown reconexion, nucleo estrecho + capacidad en bordes (herramienta->CLI+skill->plugin->MCP->core tool). (4) Biblioteca PC1 (73 repos, 187 patrones): harness engineering (modelo=motor, harness=todo), patron opencode App-as-Container + 12-factor + plugins, DMN (lethe), autoresearch, engram topic upserts, mem0 API, medusa security, LLMLingua. PLAN: reparar SuperNEXUS con estos patrones.
