# Docker Internals — OCI, runc, containerd, Namespaces, Cgroups, Image Layers, Storage, Seguridad

# Docker Internals: contenedores no son maquinas virtuales, son procesos Linux aislados. OCI (Open Con

Docker Internals: contenedores no son maquinas virtuales, son procesos Linux aislados. OCI (Open Container Initiative): estandariza runtime spec (como ejecutar contenedores) e image spec (formato de imagenes). Linux Namespaces: aislan recursos del proceso. PID namespace (procesos aislados), Network 

## Temas


## Codigo

