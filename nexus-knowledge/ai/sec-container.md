# sec-container

Container security: 1) Minimal base images (distroless, alpine). 2) Non-root user. 3) Read-only filesystem. 4) Drop all capabilities. 5) No shell access in prod. 6) Image signing (cosign). 7) Runtime security (falco). 8) Network policies.
