# dns-deep-dive

DNS deep dive: 1) Recursive resolver. 2) Root nameservers. 3) TLD servers (.com, .org). 4) Authoritative servers. 5) TTL: cache duration. 6) Record types: A, AAAA, CNAME, MX, TXT, NS. 7) DNS over HTTPS (DoH). 8) Load balancing: round-robin DNS.
