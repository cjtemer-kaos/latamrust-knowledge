# k8s-networking

K8s networking: 1) Pod-to-Pod: flat network (CNI plugin). 2) Service types: ClusterIP (internal), NodePort (external), LoadBalancer (cloud). 3) Ingress: HTTP routing, TLS termination. 4) NetworkPolicy: firewall rules between pods. 5) DNS: service discovery via CoreDNS.
