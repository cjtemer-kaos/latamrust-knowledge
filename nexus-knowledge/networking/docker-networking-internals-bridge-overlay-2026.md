# docker-networking-internals-bridge-overlay-2026

# Docker networking: kernel-level virtualization using Linux network namespaces + veth pairs + bridges

Docker networking: kernel-level virtualization using Linux network namespaces + veth pairs + bridges + iptables. Network drivers: 1) Bridge (docker0, default): creates virtual switch in kernel, containers on same bridge communicate via veth pairs - veth0 in container (eth0) + veth1 on host attached 

## Temas


## Codigo

