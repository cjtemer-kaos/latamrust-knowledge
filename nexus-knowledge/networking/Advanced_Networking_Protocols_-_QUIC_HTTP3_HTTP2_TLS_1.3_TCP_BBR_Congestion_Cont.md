# Advanced Networking Protocols - QUIC HTTP/3 HTTP/2 TLS 1.3 TCP BBR Congestion Control DNS DoH CDN IPv6 SRv6 MASQUE Cache Edge Computing HTTP Caching

# Advanced Networking & Protocols: protocolos modernos de red, TCP/IP internals, HTTP/3, QUIC y DNS. T

Advanced Networking & Protocols: protocolos modernos de red, TCP/IP internals, HTTP/3, QUIC y DNS. TCP/IP Deep Dive: TCP congestion control algorithms: NewReno (classic, AIMD), Cubic (Linux default, H-TCP, better high BDP), BBR (Bottleneck Bandwidth and Round-trip, Google 2016, model-based, achieves

## Temas


## Codigo

