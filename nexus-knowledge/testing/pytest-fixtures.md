# pytest-fixtures

Pytest fixtures: @pytest.fixture def db(): yield connection; connection.close(). Scope: function, class, module, session. conftest.py: shared fixtures. Parametrize with fixtures. Autouse: automatic fixture.
