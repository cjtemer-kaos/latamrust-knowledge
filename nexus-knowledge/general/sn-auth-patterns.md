# sn-auth-patterns

Auth in aiohttp: 1) API keys: X-API-Key header. 2) JWT: Authorization: Bearer <token>. 3) Session cookies: aiohttp_session. 4) Rate limiting: middleware counting requests. 5) CORS: aiohttp_cors. 6) Input validation: marshmallow, pydantic.
