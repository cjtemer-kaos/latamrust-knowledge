# pandas-essentials

Pandas essentials: 1) DataFrame: pd.DataFrame(). 2) Selection: df['col'], df.loc, df.iloc. 3) Filtering: df[df['col'] > 5]. 4) Groupby: df.groupby('col').agg(). 5) Merge: pd.merge(). 6) Pivot: df.pivot_table(). 7) Apply: df.apply(func). 8) Read: pd.read_csv().
