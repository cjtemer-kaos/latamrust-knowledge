# image-processing-opencv

OpenCV: 1) cv2.imread(). 2) cv2.cvtColor(). 3) cv2.resize(). 4) cv2.GaussianBlur(). 5) cv2.Canny() edges. 6) cv2.threshold(). 7) cv2.findContours(). 8) cv2.rectangle/circle. 9) cv2.putText(). 10) cv2.VideoCapture().
