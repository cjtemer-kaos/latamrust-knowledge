# sec-csrf

CSRF protection: 1) SameSite cookies (Lax/Strict). 2) CSRF tokens (synchronizer pattern). 3) Double-submit cookie. 4) Origin/Referer header checks. 5) Custom headers for AJAX. 6) Safe methods (GET, HEAD, OPTIONS) idempotent.
