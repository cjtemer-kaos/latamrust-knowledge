# ssh-advanced

SSH avanzado: 1) ssh-keygen -t ed25519: generate key. 2) ssh-copy-id user@host: copy key. 3) ~/.ssh/config: aliases. 4) ssh -L 8080:localhost:80: tunnel. 5) ssh -N: no shell. 6) ssh -f: background. 7) Agent forwarding: ssh -A. 8) Jump host: ssh -J.
