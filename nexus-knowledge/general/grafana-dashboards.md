# grafana-dashboards

Grafana dashboard design: 1) Row-based layout. 2) Variables for dynamic filtering. 3) Panel types: time series, stat, bar gauge, table. 4) Alert rules from panels. 5) Annotations for deploy markers. 6) Mixed datasource for correlation. 7) Dashboard as JSON for version control.
