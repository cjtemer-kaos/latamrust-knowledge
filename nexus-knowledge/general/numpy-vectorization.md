# numpy-vectorization

NumPy vectorization: 1) Avoid Python loops. 2) Boolean indexing. 3) Broadcasting. 4) np.where for conditional. 5) np.vectorize. 6) np.frompyfunc. 7) Memory layout optimization.
