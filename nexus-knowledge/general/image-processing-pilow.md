# image-processing-pilow

Pillow (PIL): 1) Image.open(). 2) resize((width, height)). 3) crop((left, upper, right, lower)). 4) rotate(angle). 5) filter(ImageFilter.BLUR). 6) ImageEnhance.Brightness/Contrast. 7) convert('RGB'). 8) save('output.png'). 9) thumbnail(). 10) composite().
