# docker-compose-prod

Docker Compose production: 1) replicas for HA. 2) resource limits. 3) health checks. 4) logging driver. 5) restart policy. 6) secrets. 7) networks isolation. 8) volumes for persistence.
