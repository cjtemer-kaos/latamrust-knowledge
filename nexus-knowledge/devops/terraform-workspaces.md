# terraform-workspaces

Terraform workspaces: 1) workspace list. 2) workspace new name. 3) workspace select name. 4) Isolated state files. 5) Variable overrides per workspace. 6) Use for dev/staging/prod.
