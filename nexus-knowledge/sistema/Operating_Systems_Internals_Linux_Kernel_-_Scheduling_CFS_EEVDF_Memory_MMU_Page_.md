# Operating Systems Internals Linux Kernel - Scheduling CFS EEVDF Memory MMU Page Table VFS ext4 btrfs eBPF XDP kprobe Interrupt Namespaces cgroups seccomp Kernel Rust

# Operating Systems Internals: Linux kernel arquitectura y mecanismos internos. Kernel Architecture: m

Operating Systems Internals: Linux kernel arquitectura y mecanismos internos. Kernel Architecture: monolithic (Linux, single address space, modules), microkernel (Minix, seL4, QNX minimal kernel, IPC for services), hybrid (Windows NT, macOS XNU, macOS kernel = Mach + BSD). Linux kernel source layout

## Temas


## Codigo

