# scikit-learn-pipeline

sklearn Pipeline: 1) make_pipeline(StandardScaler, LogisticRegression). 2) ColumnTransformer for different preprocessing. 3) FeatureUnion. 4) GridSearchCV with pipeline. 5) Custom transformers.
