# rest-api-versioning

API versioning: 1) URL path: /v1/resource. 2) Header: Accept-Version. 3) Query param: ?version=1. Strategy: version only breaking changes, deprecate old versions, communicate timelines.
