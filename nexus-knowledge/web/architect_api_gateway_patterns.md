# architect: api gateway patterns

# Tema 'api gateway patterns' para gema architect:

Tema 'api gateway patterns' para gema architect:

## Temas


## Codigo


---

Tema 'api gateway patterns' para gema architect:
Microservices Pattern: Pattern: API Gateway / Backends for Frontends: The API gateway must use either the Client-side Discovery pattern or Server-side Discovery pattern to route requests to available service instances.
API Gateway Pattern: 5 Design Options and How to Choose | Solo.io: May 26, 2025 - The API gateway pattern is a common architectural pattern in which an API gateway sits between the client and a collection of microservices.
API gateway pattern - AWS Prescriptive Guidance: The API gateway pattern is recommended if you want to design and build complex or large microservices-based applications with multiple client applications. The pattern is similar to the facade pattern from object-oriented design, but it is part of a distributed system reverse proxy or gateway ...
