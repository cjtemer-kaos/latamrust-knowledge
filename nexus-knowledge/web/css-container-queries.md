# css-container-queries

Container queries: 1) @container (min-width: 400px). 2) Container types: inline-size, size. 3) Responsive components. 4) @container style queries. 5) container-type: inline-size on parent. 6) Better than media queries for components.
