# fetch-api

fetch API: fetch(url, {method, headers, body}). Response: .json(), .text(), .blob(). Error handling: try/catch with !response.ok. AbortController for timeout. Headers: new Headers(). Credentials: include for cookies.
