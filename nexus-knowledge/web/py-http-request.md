# py-http-request

HTTP: import requests; r = requests.get('https://api.example.com/data'); data = r.json(); print(data['key']). POST: requests.post(url, json=payload). Headers: headers={'Auth': 'token'}.
