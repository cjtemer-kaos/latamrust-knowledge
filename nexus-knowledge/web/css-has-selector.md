# css-has-selector

CSS :has() selector: 1) Parent selector. 2) .card:has(img) styles cards with images. 3) Form validation styling: input:invalid ~ .error. 4) Empty state: div:empty { display: none }. 5) Sibling selection.
