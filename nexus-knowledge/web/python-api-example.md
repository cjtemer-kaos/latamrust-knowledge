# python-api-example

FastAPI example: from fastapi import FastAPI; app = FastAPI(); @app.get('/users/{id}'); async def get_user(id: int): return {'id': id, 'name': 'John'}. Run: uvicorn main:app --reload.
