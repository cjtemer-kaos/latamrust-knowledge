# http3-quic

HTTP/3 (QUIC): 1) Built on UDP. 2) No head-of-line blocking (even at transport level). 3) 0-RTT connection. 4) Connection migration (Wi-Fi → cellular). 5) Built-in encryption (TLS 1.3). Benefits: faster, more reliable on unstable networks. Cloudflare, Google support.
