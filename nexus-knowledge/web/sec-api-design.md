# sec-api-design

Secure API design: 1) Authentication on every endpoint. 2) Authorization checks (RBAC). 3) Input validation + sanitization. 4) Rate limiting per user/IP. 5) Request size limits. 6) CORS whitelist. 7) Error messages: generic, no stack traces. 8) Logging: who, what, when.
