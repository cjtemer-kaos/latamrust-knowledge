# Kubernetes Internals — Scheduler, CNI, Controllers, Operators, CRDs, kube-apiserver, etcd

# Kubernetes internals: arquitectura de scheduler, CNI, controllers, operators, CRDs. Scheduler: asign

Kubernetes internals: arquitectura de scheduler, CNI, controllers, operators, CRDs. Scheduler: asigna Pods a Nodes basado en filtering (node resources, nodeSelector, affinity, taints/tolerations) y scoring (least requested, most requested, balanced resource). Scheduling plugins: PodTopologySpread, N

## Temas


## Codigo

