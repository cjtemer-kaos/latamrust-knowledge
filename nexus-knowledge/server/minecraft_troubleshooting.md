# minecraft_troubleshooting

Minecraft server troubleshooting: crash logs in logs/latest.log. Common issues: out of memory (increase -Xmx), port conflict (change server-port), missing mod dependency, version mismatch. Use /debug to diagnose performance.
