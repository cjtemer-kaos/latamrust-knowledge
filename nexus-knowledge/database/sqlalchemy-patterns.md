# sqlalchemy-patterns

SQLAlchemy patterns: 1) Engine + Session. 2) Declarative models: Base = declarative_base(). 3) Relationships: relationship('User'). 4) Queries: session.query(Model).filter(). 5) Migrations: Alembic. 6) Connection pooling. 7) Async: async SQLAlchemy. 8) Bulk operations.
