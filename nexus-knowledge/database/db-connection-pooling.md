# db-connection-pooling

Connection pooling: 1) PgBouncer: PostgreSQL proxy. 2) Pgpool-II: load balancing. 3) SQLAlchemy pool: QueuePool, NullPool. 4) HikariCP (Java). 5) min/max connections. 6) Connection timeout. 7) Pool monitoring. 8) Why: DB connections are expensive.
